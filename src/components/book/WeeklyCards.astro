---
import type { WeeklyCardItem } from './special-pages';
import WeeklyCardsView from './WeeklyCardsView';
interface Props { items: WeeklyCardItem[]; }
const { items } = Astro.props;
---
<WeeklyCardsView items={items} client:visible />
